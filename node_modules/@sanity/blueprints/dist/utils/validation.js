export function runValidation(validator) {
    const errors = validator();
    if (errors.length > 0) {
        const message = errors.map((err) => err.message).join('\n');
        throw new Error(message);
    }
}
//# sourceMappingURL=validation.js.map